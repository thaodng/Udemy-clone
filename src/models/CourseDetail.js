class CourseDetail {
  constructor({
    id,
    idCourse,
    idSection,
    title,
    duration,
    videoUrl
  }) {
    this.id = id;
    this.idCourse = idCourse;
    this.idSection = idSection;
    this.title = title;
    this.duration = duration;
    this.url = url;
  }
}

export default CourseDetail;
