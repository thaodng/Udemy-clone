class CourseDetail {
  constructor({
    id,
    idCourse,
    idSection,
    title,
    duration
  }) {
    this.id = id;
    this.idCourse = idCourse;
    this.idSection = idSection;
    this.title = title;
    this.duration = duration;
  }
}

export default CourseDetail;
